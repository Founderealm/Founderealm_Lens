import java.util.List;
import java.util.Map;
public class Zeta {
    private int id;
    public int run() { return id; }
    public static void helper() { new Zeta().run(); }
}
interface Runner { int run(); }
