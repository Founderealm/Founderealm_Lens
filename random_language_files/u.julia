using DataFrames

# Define a simple struct-like type (Julia uses structs for this)
struct Delta; value::Int; end

function run(d::Delta)
    return d.value
end

function helper()
    Delta(99).value
end
